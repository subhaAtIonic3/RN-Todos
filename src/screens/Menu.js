import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Menu = () => {
  return (
    <View>
      <Text>Menu Tab</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Menu;
